#include<stdio.h>
#include<stdlib.h>

/**
 * fileTest
 * makes sure the can be found
 * IN: takes in the file
 * OUT: returnes zero if succesful else it exits with 1
 * POST: no known sidef effects
 * ERROR: no knwon errors
 */
int fileTest(char **argv);
